services:
  web:
    build: .
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    environment:
      - SECRET_KEY=django-insecure-temp-key-for-deploy
      - DEBUG=True
      - ALLOWED_HOSTS=*